Open command prompt in the directory where source file resides
Ensures that text file  and gson.jar file also there.

Now follow the step to compile and run the project.

>> For compilation type
	javac -cp gson.jar BlockChain.java

>> To run 
	java BlockChain limit 4 data new-ledger.txt